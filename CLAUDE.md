# 个人网站维护规范

## 项目定位

- 本仓库是 `https://www.caiyanming.com/` 的单页静态网站。
- 唯一事实口径：蔡炎洺 **2024 年起做 AI 产品**，此前做搜索策略产品（2023 年起）。不得写成“24 年产品经验”或“做了 24 年产品”，也不得写成“2024 年才开始做产品”。

## 文件结构

- `index.html`：线上页面的 HTML、CSS 与 JavaScript；保持单文件部署结构。
- `assets/content/`：网站公开提供下载的文章与演示文稿，文件名使用小写英文和连字符。
- `assets/images/notes/`：精选笔记卡片配图，优先复用原始内容中的图像或封面。
- `assets/images/profile/`：个人肖像与关于页照片，文件名使用小写英文和连字符；页面引用时必须提供准确的 `alt` 文本。
- `网站框架.md`：网站定位、叙事与内容结构说明。
- `diagrams/`：设计过程中的图示资料，不作为线上运行依赖。
- 临时截图、测试产物不得放进仓库根目录；验证后及时清理。

## 修改约定

- 页面文案与 `网站框架.md` 的事实口径保持一致。
- 首屏现有视频、四季切换器与其动效属于锁定区域；整站改版时不得改变其行为，只能调整首屏以下的章节衔接。
- 首屏文案同属锁定区域，仅在事实口径出错时、经用户当次明确确认后才可修改（2026-09 据此更正为「2024 年起做 AI 产品」）。
- 首屏以下按四季氛围分配动态背景：关于我使用“破晓 / 春”，正在思考使用“深林 / 夏”，精选笔记使用“金色时刻 / 秋”，联系与页脚使用“静水 / 冬”。
- 视觉系统统一使用深色底、衬线展示字体、无衬线正文字体、液态玻璃卡片与克制的渐入动效；不得让首屏丰富、后续页面连续纯黑。
- 个人经历一律以本人简历为准，不得虚构或沿用来源不明的旧文案；引用经历时必须抹掉公司名称与项目名称，改用描述性说法（如「一家内容社区平台」「面向教研管理层的 AI 工具」）。
- 手机号、身份证、住址等隐私信息不得出现在页面或仓库中；公开联系方式仅限邮箱与小红书账号。
- 不在未确认时替换用户已有的设计、媒体链接或未提交修改。
- 新增外部资源时优先使用 HTTPS，并为图片提供 `alt`，为交互控件提供可访问名称。
- 不把密钥、Token、密码或个人隐私数据写入代码。

## 验证

- 修改后检查 HTML 中不存在错误口径：`rg -n "24年经验|做了24年|过去24年|为什么24年后" index.html 网站框架.md`。
- 用本地静态服务器打开 `index.html`，至少验证桌面首屏、移动端导航、页内锚点与浏览器控制台错误。
- 发布后访问正式域名，确认标题、首屏文案与刷新后的页面均为新版本。

## 发布边界

- `git push` 或任何公开部署必须先取得用户当次明确确认。
